import React from 'react';
import PropTypes from 'prop-types';
import ListBlog from './ListBlog';

Blog.propTypes = {
    
};

function Blog(props) {
    return (
        <div>
            <ListBlog />
        </div>
    );
}

export default Blog;