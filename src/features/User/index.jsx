import React from 'react';
import PropTypes from 'prop-types';
import ListUser from './pages/ListUser';

User.propTypes = {
    
};

function User(props) {
    return (
        <div>
            <ListUser />
        </div>
    );
}

export default User;